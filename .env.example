EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_APP_ENV=development

# Google Auth: enable the Google provider in Supabase Dashboard → Authentication → Providers.
# Add redirect URLs for local web, production, and elplay:// (native).
