// Renombrado a register.tsx — mantener este archivo vacío para no romper git history
import { Redirect } from 'expo-router'
export default function Registro() {
  return <Redirect href="/(auth)/register" />
}
